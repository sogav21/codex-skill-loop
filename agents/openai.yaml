interface:
  display_name: "/loop"
  short_description: "Completion and research loops with verification"
  default_prompt: "Use $loop to keep iterating until the task is achieved, verified, or measurably improved."
