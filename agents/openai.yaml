interface:
  display_name: "Loop"
  short_description: "Iterate until the task is verified done"
  default_prompt: "Use $loop to keep iterating on this task until the requested outcome is achieved and verified."
