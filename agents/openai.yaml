interface:
  display_name: "/loop"
  short_description: "Verified loops with experiments and revert"
  default_prompt: "Use $loop to keep iterating with verified attempts, experiments, and revert decisions until the target is reached."
